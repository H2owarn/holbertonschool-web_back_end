#!/usr/bin/env python3
"""
Module takes a list mxd_lst of integers
and floats and returns their sum as a float.
"""

def sum_list(input_list: float) -> float:
    """Return sum
        Args:
        input_list (flat): The input list.

    Returns:
        sum
    """
    return sum(input_list)
